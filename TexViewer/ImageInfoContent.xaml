<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TexViewer.ImageInfoContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TexViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollView Width="400"
        VerticalAlignment="Top" HorizontalAlignment="Left"
        HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Visible">
        <StackPanel>
            <StackPanel Margin="0,0,8,0"
                        BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="8">
                <TextBlock x:Name="GeneralInfo" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Margin="0,8,8,0"
                        BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="8">
                <TextBlock x:Name="LoaderInfo" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Margin="0,8,8,0"
                        BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="8">
                <TextBlock Text="System/Display Information"
                           Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock x:Name="TexViewerInfo" TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </ScrollView>
</Page>
